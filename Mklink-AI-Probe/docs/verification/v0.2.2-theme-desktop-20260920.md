# 0.2.2 Web/desktop themes

## Scope

The shared Vue interface now offers System, Light and Dark themes in the header.
The preference is saved locally; system changes apply only in System mode.
SuperWatch and SystemView canvases redraw their backgrounds, axes and text when
the theme changes. Signal colors and active acquisitions are preserved.

RTT and the serial assistant share a transmit bar. Its wrapper now uses the
surface token, removing the white surround reported in dark mode. Forms,
status badges and dialogs also use semantic theme colors. The update history
includes the theme feature. Plugin and Site Agent version metadata are aligned
with the existing 0.2.2 application version.

## Verification

- Frontend: 716 tests in 68 files passed; production build passed.
- Chrome: live HPM6E80 three-variable SuperWatch at 30 MHz continues across
  light/dark changes. Preference survives a reload. Canvas background pixels
  are opaque dark (9,12,16,255) or light (250,249,245,255).
- Chrome: dark RTT transmit bar and offline download forms inspected visually;
  transmit wrapper background is RGB(25,29,36), with no white surround.
- Full Python run: 2257 passed, 2 skipped, 18 failed and 3 errors before repair.
  Twelve failures concern unavailable Windows symbolic-link privileges. Other
  failures identified stale version metadata and SystemView clock-test fixtures
  missing the trusted RAM map now required by the RTT search path. After repair,
  all 28 affected tests passed, including a fresh isolated Site Agent package
  build and archive audit. The 12 privilege-dependent failures remain an
  environment limitation; this is not a claim of full-suite qualification.
- Local offline NSIS built successfully from source `cdbf08051a53`, with fresh
  bundled Python sidecar and offline WebView2. Builder validated 7,059 targets
  and 2,224 FLM blobs. No public Skill ZIP was requested or regenerated.
- Actual overwrite installation from 0.2.1 returned 0 with PATH restricted to
  Windows directories; installed registry version is 0.2.2. Native health and
  probe enumeration returned HTTP 200. The process tree contains the application
  and its bundled sidecar, with no Python interpreter process.
- Native dark theme and RTT transmit bar inspected; theme survives closing and
  reopening the application. Normal close leaves no MKLink processes or listener
  on port 8765.
- Installed backend rejects HPM HardFault decoding with the Cortex-M-only
  message. Target wave_tick continued from 2392668 to 2393211 across the check.
  Old development backend 8770 was not restarted; its device session was released
  before the installed backend connected.

## Local installer

`Mklink-AI-Probe-v0.2.2-cdbf08051a53-x64-Offline-Setup.exe` (290.7 MiB).
SHA-256: `ccc5f4fae21fdaca150479436a841995a0a4030ab2bf693f0f3d8e5efe35b3d6`.
Stored under `.build/artifacts/theme-022-20260920`.
This is an unsigned local installer (Windows may show an unknown publisher),
without an updater signature. No release or updater channel was changed.

Local evidence is under `.build/reports/theme-022`; screenshots and installers
are not committed to the source repository.
