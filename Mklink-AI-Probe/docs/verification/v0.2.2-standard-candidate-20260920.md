# 0.2.2 标准安装包候选验收

状态：开发候选，正式发布暂停。2026-09-20 用户要求保留 HPM 脱机 OTP
开发草稿，等待补验后再决定发布。不得将本报告解释为合并、签名或发布授权。

## 本轮范围

- 改回标准 NSIS，不嵌入 WebView2 离线安装器；已有 WebView2 时复用，缺失时
  沿用标准安装流程获取运行环境。这个包不承诺离线补齐所有系统依赖。
- WebGUI 与桌面版首次启动默认暗黑。已有浅色或跟随系统选择继续保留；
  设置缺失、损坏或存储读取受限时回退暗黑。
- HPM 脱机 OTP 草稿原样保留，其校验门槛、失败注入及 HPM5301 真机流程
  仍按 [交接说明](../ai/hpm-offline-otp-handoff.md) 待验收。

## 架构保护补验

在已安装 0.2.2 的独立后端 8765 上连接 HPM6E80，停止 SuperWatch 后，
实际点击原生 GUI 的 HardFault 检查。右上角显示
`HardFault decoding requires a Cortex-M target`，已保存截图。
此前该后端 API 检查同时确认目标 wave_tick 仍在增长。由此补齐 GUI 提示
验收，不再仅引用 API 结果。开发服务器 8770 未在本轮重启。

## 当前结果

本轮原始全量 Python：2266 passed、2 skipped，12 项 Windows symlink 权限失败经提升权限独立重跑全部通过。前端后续修复后的 718 项及生产构建通过，见 online-verify-theme 报告。Rust 测试程序以 0xc0000139（入口点缺失）退出，标准安装包构建链未继续执行；没有生成或覆盖安装新的标准包。正式发布保持暂停。
本地证据：`.build/reports/release-022-final`。不提交截图、安装包或硬件标识。
