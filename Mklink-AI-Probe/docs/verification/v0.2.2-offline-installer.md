# 0.2.2 offline test installer

Source commit: 1c2c99a. NSIS x64 local test bundle, no updater signature or publication.

- Build completed successfully with a fresh PyInstaller backend and production GUI.
- Installer: 304654762 bytes (290.54 MiB).
- Full Microsoft WebView2 offline runtime embedded by NSIS offlineInstaller configuration: 212925648 bytes; Microsoft Authenticode signature valid.
- Backend archive verified: 7059 targets, 2224 algorithm blobs, SansIO WebSocket implementation included.
- Chinese package update notes and SHA-256 file accompany the installer; bilingual updates are built into the app.
- Package checksum: 2f8b0eae534d29db783d7d9d205333101850c0e8203d699bdabbff46a29cfff0  Mklink-AI-Probe-v0.2.2-1c2c99a-x64-Offline-Setup.exe

Validation limitation: the attempted per-machine overwrite installation returned Windows error 'operation canceled by user' at UAC. No new installation success is claimed. Build-output startup is tested separately with a Windows-only PATH; this does not replace installation qualification. The machine already has WebView2, so first installation on a clean offline OS remains unverified. The executable is unsigned and may show Unknown Publisher. Online updates and new resource downloads still require a network; external Packs may be imported offline.

The installer and raw evidence remain under ignored build artifacts/reports; official update channels are unchanged.

Build-output runtime checks passed: Windows-only PATH, health=ok, bundled ELF available, probe enumeration successful, WebSocket ping/keepalive while consuming events for 22 seconds, and no Python executable in the app child tree. Normal window close removed application/sidecar processes and released port 8765. An initial idle test stopped reading events and exhausted the client receive queue, causing its second ping timeout; the consumer test corrected this harness error. No installation success is inferred from these build-output checks.
